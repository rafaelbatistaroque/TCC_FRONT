import React from 'react';
import styles from './index.module.css';

const ColaboradorAlterar = ({ alterarColaborador }) => {

    return <div className={`${styles.index} container`}>ColaboradorEditar</div>;

};

export default ColaboradorAlterar;