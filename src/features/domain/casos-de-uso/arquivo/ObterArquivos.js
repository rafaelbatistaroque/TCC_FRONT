import { TEXTOS } from "../../../../utils/constantes";

export default class ObterArquivos {
    handler() {
        throw new Error(TEXTOS.NAO_IMPLEMENTADO);
    }

    validar() {
        throw new Error(TEXTOS.NAO_IMPLEMENTADO);
    }
}
